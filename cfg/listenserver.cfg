sv_pure -1
sv_allow_point_servercommand always
//this command will always allow it for tr_walkway bug to fix, allowing the training bots to spawn
//the first command will allow it for adding weapon overhauls to work in valve servers
